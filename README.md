Most works contains the code with comments, and the gtest files. We used google test framework to run our tests for every task.
I deleted packages and bin libraries to upload just the necessary files.
